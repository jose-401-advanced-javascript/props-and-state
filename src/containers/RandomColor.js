import React, { Component } from 'react';

export default class RandomColors extends Component {
  state = {
    backgroundColor: 'black'
  };

  render() {

    return (
      <dl>console.log(state)</dl>      
    );
  }
}
